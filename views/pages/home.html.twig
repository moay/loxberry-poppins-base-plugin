{% extends 'pages/adminBasePage.html.twig' %}

{% block htmlHead %}
<script>console.log('test');</script>
{% endblock %}

{% block body %}
    <img src="{{ assetUrl('img/poppins-logo.png') }}" />
    <h2>{{ trans('demo.headline') }}</h2>
    <p>{{ trans('demo.explanation_1')|raw }}</p>
    <p>{{ trans('demo.explanation_2') }}</p>
    <p>{{ trans('demo.have_fun') }}</p>


    {{ logListButton('Cron', 'buttons.logfile') }}
    {{ logFileButton('Cron', 'buttons.logfile') }}
    {{ miniserverSelect('labels.miniserver') }}
    {{ logLevelSelect() }}
    <h2>{{ plugin.name }}</h2>
    <h3>{{ plugin.version }}</h3>
    <div>{{ 'test.translation'|trans }}</div>
    <div>{{ 'test.translation'|translate }}</div>
    <div>{{ trans('test.translation2') }}</div>
    <div>{{ translate('test.translation2') }}</div>

    <form method="POST">
        <input type="text" value="{{ fieldValue }}" name="newFieldValue" />
        <button type="submit">{{ 'form.save'|trans }}</button>
    </form>
{% endblock %}
